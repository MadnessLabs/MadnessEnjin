ion-view.page-settings(view-title='Settings')
    ion-nav-bar.bar-positive.has-tabs-top
        ion-nav-buttons(side="left")
            a.button.button-icon.icon.ion-arrow-left-c(ng-click='$root.back()')
    ion-content
        .card
            ion-list
                ion-item
                    h2 Name
                    p {{$root.session.user.displayName}}
                ion-item
                    h2 Email
                    p {{$root.session.user.email}}
                a.item.item-icon-right(clipboard text='$root.session.token')
                    h2 Secret Token
                    p DO NOT SHARE THIS (Click to Copy)
                    i.icon.ion-android-clipboard
        .padding
            a.button.button-block.button-positive.icon-left.ion-power(ng-click='$root.logout()') Logout